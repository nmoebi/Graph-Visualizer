# Graph-Visualizer
A visualizer for user input graphs.

The user can insert vertices and edges between vertices.

On the resulting graph, exact algorithms for the following graph-coloring-problems can be used:
  - Classical Vertex Coloring
  - Vertex Sum Coloring
